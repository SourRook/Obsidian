# System

This folder contains one file: [[CLAUDE.md]] — the governance document for the Matteo-Claude partnership.

Everything else lives in the knowledge layer (the vault) or the skills layer (`.claude/skills/`).

#system #index
